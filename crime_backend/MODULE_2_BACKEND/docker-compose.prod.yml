version: '3.8'

# Production override file to hide internal database/redis ports.
# Usage: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  postgres:
    ports: []

  neo4j:
    ports: []

  redis:
    ports: []
